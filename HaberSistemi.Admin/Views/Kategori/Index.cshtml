@model IEnumerable<HaberiSistemi.Data.Model.Kategori>
@{
    ViewBag.Title = "Kategori Listesi";
}
<div class="container-fluid">
    <div class="row">
        <!-- Responsive table -->
        <div class="col-md-12">
            <div class="panel panel-default">
                <header class="panel-heading">@ViewBag.Title</header>
                <div class="panel-body">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Kategori Adı</th>
                                    <th>Üst Kategori</th>
                                    <th>Durum</th>
                                    <th>Ekleyen</th>
                                    <th>İşlemler</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var kategori in Model)
                                {
                                    <tr>
                                        <td><span class="label label-success">@kategori.Id</span></td>
                                        <td>@kategori.KategoriAdi</td>
                                        <td>@kategori.ParentId</td>
                                        <td>@Html.Raw(kategori.Aktif ? "<span class='label label-success'>Aktif</span>" : "<span class='label label-danger'>Pasif</span>")</td>
                                        <td><a href="#">@kategori.Kullanici.Id</a></td>
                                        <td>
                                            <button class="btn btn-primary btn-sm">Düzenle</button>
                                            @Html.ActionLink("Sil", "Sil", new {id = kategori.Id}, new {onclick="return confirm('Kaydı silmek istediğinize emin misiniz?')", @class="btn btn-danger btn-sm"})
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
        <!-- /Responsive table -->
    </div>
</div>

